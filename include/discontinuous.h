#include <vector>

namespace launchrange {
    // helper functions for using std::vector<launchrange::range_<T>>

    template<typename T>
    std::vector<launchrange::range_<T>> add_dc_range() {
        return { };
    }

    template<typename T>
    std::vector<launchrange::range_<T>> subtract_dc_range() {
        return { };
    }

    std::vector<launchrange::range_<T>>
}
